%web_drop_table(WORK.IMPORT);
FILENAME REFFILE '/home/u61360193/sasuser.v94/projekt3_dane.xls';
PROC IMPORT DATAFILE=REFFILE
	DBMS=XLS
	OUT=WORK.IMPORT;
	GETNAMES=YES;
RUN;
PROC CONTENTS DATA=WORK.IMPORT; RUN;
%web_open_table(WORK.IMPORT);
PROC FREQ data=WORK.IMPORT;
    TABLE clinstg*chemo / CHISQ;
RUN;
PROC FREQ data=WORK.IMPORT;
    TABLE clinstg*age_kat / CHISQ;
RUN;
PROC FREQ data=WORK.IMPORT;
    TABLE clinstg*hgb_kat / CHISQ;
RUN;
PROC FREQ data=WORK.IMPORT;
    TABLE chemo*age_kat / CHISQ;
RUN;
PROC FREQ data=WORK.IMPORT;
    TABLE chemo*hgb_kat / CHISQ;
RUN;
PROC FREQ data=WORK.IMPORT;
    TABLE age_kat*hgb_kat / CHISQ;
RUN;
proc lifetest data= WORK.IMPORT plots=(s) outsurv=surv_ci;
time dftime*dfstat(0);
strata clinstg / test=(logrank);
run;
proc lifetest data= WORK.IMPORT plots=(s) outsurv=surv_ci;
time dftime*dfstat(0);
strata chemo / test=(logrank);
run;
proc lifetest data= WORK.IMPORT plots=(s) outsurv=surv_ci;
time dftime*dfstat(0);
strata age_kat / test=(logrank) trend;
run;
proc lifetest data= WORK.IMPORT plots=(s) outsurv=surv_ci;
time dftime*dfstat(0);
strata hgb_kat / test=(logrank) trend;
run;
proc lifetest data= WORK.IMPORT plots=(s) outsurv=surv_ci;
time dftime*dfstat(0);
strata clinstg /group=chemo test=(logrank);
run;
proc lifetest data= WORK.IMPORT plots=(s) outsurv=surv_ci;
time dftime*dfstat(0);
strata chemo /group=clinstg test=(logrank);
run;
proc lifetest data= WORK.IMPORT plots=(s) outsurv=surv_ci;
time dftime*dfstat(0);
strata clinstg /group=age_kat test=(logrank) trend;
run;
proc lifetest data= WORK.IMPORT plots=(s) outsurv=surv_ci;
time dftime*dfstat(0);
strata age_kat /group=clinstg test=(logrank);
run;
